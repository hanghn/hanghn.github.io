/**
 * Background animation slot. Currently disabled — leave the file
 * in place so layout.tsx still has something to mount, then swap
 * the body back in if we ever want the floating symbols again.
 */
export default function CodeBackground() {
  return null;
}
